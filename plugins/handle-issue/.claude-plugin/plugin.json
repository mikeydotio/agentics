{
  "name": "handle-issue",
  "version": "2.25.1",
  "description": "Dispatch a GitHub issue to a fresh, plan-mode Claude session in a new tmux window and per-issue git worktree — pick an issue by number or from open issues, then auto-launch claude -w <repo-prefix>-<n> (worktree named like the window) and prompt it to plan a fix",
  "author": {
    "name": "mikeydotio"
  }
}
