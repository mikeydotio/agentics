# agentics — Atlas Codebase Map

Generated by atlas at baseline 17e6ecc. Derived file — do not edit by hand; regenerate with /atlas update.
Module docs live at docs/atlas/modules/<id>.md — before working in an area, read its module doc.

## Modules

| Module | When working on |
| --- | --- |
| plugins-agents-agents-chunk-1 | Adding or changing shared agents A–H, the agent template schema, or shared guardrails |
| plugins-agents-agents-chunk-2 | Touching the shared agents investigator through technical-writer (incl. map-verifier) |
| plugins-agents-agents-chunk-3 | Touching forge triage/validate steps or the triager/validator agent contracts |
| plugins-agents-agents-ux | Choosing or editing a platform-specific UX reviewer (CLI, mobile, or web) |
| plugins-agents-misc | Adding agents, changing the agent frontmatter contract, or wiring /agents commands |
| plugins-agents-references | Choosing/staffing shared agents, writing agent-overrides, or auditing plugin tool design |
| plugins-atlas-agent-overrides | Changing atlas agent behavior, spawn constraints, or repair/verify protocols |
| plugins-atlas-chunk-1 | Touching atlas plugin identity, README, or user-facing command reference |
| plugins-atlas-chunk-2 | Touching atlas-cli subcommands, /atlas orchestration, the session hook, or blob ledger |
| plugins-atlas-references | Changing atlas behavior, map format, orchestration steps, or the CLAUDE.md block |
| plugins-atlas-tests-chunk-1 | Changing atlas-cli or hook behavior, or writing/debugging atlas plugin tests |
| plugins-atlas-tests-chunk-2 | Changing atlas-cli or hook behavior, or writing/debugging atlas plugin tests |
| plugins-council | Touching /council-vote, the council protocol, voting/IRV mechanics, or panel selection |
| plugins-deployit-assets | Changing deployit's install pages, OTA manifest, export signing options, or config seed |
| plugins-deployit-bin | Changing deployit subcommands, the deploy/publish flow, or backend endpoints |
| plugins-deployit-misc | Changing /deployit command surface, deploy/semver protocol, or router invocation rules |
| plugins-deployit-references | Changing deployit deploy/bootstrap/proxy/semver behavior or debugging install failures |
| plugins-deployit-tests-chunk-1 | Touching deployit backend routes/UI, CLI bootstrap/bump/preflight, or its tests |
| plugins-deployit-tests-chunk-2 | Changing deployit redeploy, gc, metadata extraction, or verifying a live deployment |
| plugins-forge-agent-overrides | Changing forge subagent behavior, .forge/ artifact contracts, or spawn constraints |
| plugins-forge-bin | Changing forge state detection, step transitions, execute prechecks, or .forge archiving |
| plugins-forge-hooks | Touching forge session resume, stop checkpointing, or .forge state hook behavior |
| plugins-forge-misc | Changing forge's marketplace identity or updating the README pipeline overview |
| plugins-forge-references | Changing forge pipeline behavior — execution, handoffs, locking, severity, or storyhook |
| plugins-forge-skills | Changing forge pipeline steps, .forge/ artifacts, routing, or freshen/step-exit handoffs |
| plugins-freshen | Touching context clearing, .freshen signal files, or /clear re-invocation automation |
| plugins-greenlight | Touching tool-call safety gating, greenlight.sh, its config schema, or /greenlight |
| plugins-hook-guard | Touching Stop hooks, stop_guard_check/stop_guard_reset, or hook-loop protection |
| plugins-rca | Touching the /rca skill, phase artifacts (.rca/<slug>/), references, or agent overrides |
| plugins-semver-hooks | Touching semver hooks, push-nudge or session version context, or .semver/hooks execution |
| plugins-semver-misc | Touching /semver commands, bump/changelog/validate logic, or the CLI JSON contract |
| plugins-semver-references | Changing semver file formats, bump/validate/hook contracts, or the CLAUDE.md block |
| plugins-semver-tests | Adding or debugging semver tests, or reusing the source-and-discover bash harness |
| root-misc | Registering/installing plugins, changing root agent instructions, or wiring make test |
| tests | Touching tests/*.bats, the bats helpers, or pilot/storyhook state-file schemas |
| overview/ARCHITECTURE | Architecture and cross-module relationships — read docs/atlas/overview/ARCHITECTURE.md |

## Key facts

- `.claude-plugin/marketplace.json` is the sole plugin registry; plugins live under `plugins/<name>/`
- Plugins: agents, atlas, council, deployit, forge, freshen, greenlight, hook-guard, rca, semver
- `plugins/agents/agents/` is the shared agent library; forge, rca, atlas, and council spawn from it
- Spawning inlines the shared agent definition plus the plugin's `agent-overrides/<x>-context.md`
- State = artifact dirs: `.forge/` `.rca/<slug>/` `.semver/` `.atlas/` `.freshen/`; resume derives from files
- Skills are thin routers; bin/ scripts (bash+jq or stdlib-only python3) own deterministic work
- CLIs emit one JSON object with `ok` + `display` per run; skills halt and show `display` on `ok:false`
- Hooks stay silent when their plugin is inactive and build all JSON with jq, never printf
- Stop hooks source hook-guard's `stop_guard_check`; a SessionStart reset re-arms the breaker
- `freshen` delivers `/clear` + re-invocation only via tmux `send-keys`; outside tmux it no-ops
- Forge steps exit uniformly: write artifact + handoff, commit, queue freshen, STOP
- Forge triage: FIX re-enters at plan (max 3 cycles, 10 yolo); ESCALATE pauses for the user
- Atlas maps update incrementally via the blob-SHA ledger; unchanged docs never reach an LLM
- Tests are mock-free bash harnesses driving real CLIs in throwaway `/tmp` git repos
- `make test` aggregates the semver, deployit, atlas, and root bats suites; pre-push runs it

## Reminders

- `.claude-plugin/marketplace.json` is the sole plugin registry; plugins live under `plugins/<name>/`
- Plugins: agents, atlas, council, deployit, forge, freshen, greenlight, hook-guard, rca, semver
- Read the routed module doc before editing unfamiliar modules; workflow and commands live in CLAUDE.md, not here.
