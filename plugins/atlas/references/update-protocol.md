# Incremental Update Protocol (`/atlas update`) — STUB

> Phase 6 deliverable — not yet implemented. If a user invokes
> `/atlas update` or `/atlas verify` before this protocol lands, say so
> plainly and offer `/atlas map` (full remap) or `/atlas status` instead.
> Do not improvise an incremental flow; hash-gated regeneration has exact
> ordering requirements that this document will specify.
