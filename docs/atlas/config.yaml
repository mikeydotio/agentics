# Atlas configuration for the agentics marketplace repo.
project_name: agentics

# Project-management data, settings, and release-churn files carry no
# mappable code structure.
exclude:
  - ".planning/**"
  - ".storyhook/**"
  - ".claude/**"
  - "CHANGELOG.md"
  - "VERSION"
  - "**/.gitkeep"
